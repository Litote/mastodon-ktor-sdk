# The mcp-server module uses custom intermediate source sets (mcpMain, serverNativeMain, serverUnixMain)
# that require explicit dependsOn() calls, so the default KMP hierarchy template is disabled.
kotlin.mpp.applyDefaultHierarchyTemplate=false
